RewriteEngine On

# Force https and non-www
RewriteCond %{HTTPS} off [OR]
RewriteCond %{HTTP_HOST} ^www\. [NC]
RewriteRule ^(.*)$ https://policy.co.ke/$1 [L,R=301]

# Send the bare root to the Kenya homepage with a proper 301
RewriteRule ^$ /kenya/index.html [L,R=301]
